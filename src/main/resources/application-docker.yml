spring:
  datasource:
    username: axis
    password: password
    url: jdbc:postgresql://postgres:5432/axis
  jpa:
    hibernate:
      ddl-auto: 'create-drop'
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true